<?php

// Staging area for MINOR fixes only (small bug fixes, tiny tweaks) — NOT
// shown in the app anywhere. Major features/releases still go straight into
// whats_new.php as before.
//
// Rule: on every "pd", if the fix is minor, add one plain-Bangla bullet here
// instead of whats_new.php. Once this list reaches 3+ items, bundle them all
// into a single new entry at the top of whats_new.php (version +0.1, today's
// date) and empty this array back out.
return [
];
